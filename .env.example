# up.sh generates a real .env (with random values) on first run — you do NOT
# need to create this by hand. This file just documents the variables.
#
# Demo-user passwords (browser login + CLI token):
ALICE_PASSWORD=change-me
BOB_PASSWORD=change-me
CAROL_PASSWORD=change-me
# Secret for the password-grant test client (scripts/get_token.sh, smoke_test.py):
MCP_TEST_CLIENT_SECRET=change-me
# Postgres password for the in-container database:
POSTGRES_PASSWORD=change-me
