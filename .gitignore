# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.uv/

# Locally-generated credentials — never committed (.env.example IS tracked)
.env
.env.local
!.env.example

# Rendered Keycloak realm (built from realm.template.json at bring-up)
keycloak/import/*.json

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Logs / runtime
*.log

# Internal build brief — not published (could be ingested by a coding agent)
docs/PORTFOLIO_HANDOFF.md
